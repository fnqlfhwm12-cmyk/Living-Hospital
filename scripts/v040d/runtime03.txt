const v040dLegacyDrawRoom=drawRoom;
drawRoom=function(zone){
 v040dLegacyDrawRoom(zone);if(!zone.structureRoom)return;const screen=worldToScreen(zone.x-zone.w*.5,zone.y-zone.h*.5),plan=structureEncounterPlans.get(zone.id),pulse=.5+.5*Math.sin(elapsed*2.4+zone.x*.003);
 ctx.save();if(zone.v040dMinor==='blackout'){ctx.fillStyle='rgba(0,0,0,.34)';ctx.fillRect(screen.x,screen.y,zone.w,zone.h);}if(zone.v040dMinor==='flood'){ctx.strokeStyle='rgba(150,178,182,.14)';ctx.lineWidth=2;for(let i=0;i<5;i++){const y=screen.y+zone.h*(.18+i*.16)+Math.sin(elapsed*1.3+i)*5;ctx.beginPath();ctx.moveTo(screen.x+40,y);ctx.lineTo(screen.x+zone.w-40,y);ctx.stroke();}}if(zone.v040dMinor==='bio'){ctx.strokeStyle='rgba(151,48,63,'+(.15+.08*pulse)+')';ctx.lineWidth=3;for(let i=0;i<4;i++){ctx.beginPath();ctx.moveTo(screen.x,screen.y+zone.h*(.18+i*.20));ctx.bezierCurveTo(screen.x+zone.w*.32,screen.y+zone.h*(.10+i*.17),screen.x+zone.w*.64,screen.y+zone.h*(.28+i*.13),screen.x+zone.w,screen.y+zone.h*(.20+i*.18));ctx.stroke();}}
 if(plan?.pools){for(const pool of plan.pools){const p=worldToScreen(pool.x,pool.y);ctx.fillStyle='rgba(93,20,35,.34)';ctx.beginPath();ctx.arc(p.x,p.y,pool.r*(.95+.04*Math.sin(elapsed*2+pool.x)),0,Math.PI*2);ctx.fill();ctx.strokeStyle='rgba(197,67,85,.42)';ctx.stroke();}}
 if(plan?.rare==='sterilization'&&plan.startedAt){const cycle=(elapsed-plan.startedAt)%4.5,active=cycle>=1&&cycle<1.55,alpha=active?.30:cycle<1?.08+.12*(cycle):0;const stripe=plan.stripe;if(stripe){ctx.fillStyle='rgba(230,244,238,'+alpha+')';if(stripe.vertical){const x=screen.x+zone.w*(.5+stripe.offset)-70;ctx.fillRect(x,screen.y,140,zone.h);}else{const y=screen.y+zone.h*(.5+stripe.offset)-55;ctx.fillRect(screen.x,y,zone.w,110);}}}
 if(plan?.rare==='compression'&&plan.startedAt){const cycle=(elapsed-plan.startedAt)%6,amount=cycle<2?0:cycle<3.5?(cycle-2)/1.5:cycle<5?(5-cycle)/1.5:0,band=amount*135;ctx.fillStyle='rgba(91,20,29,'+(.14*amount)+')';ctx.fillRect(screen.x,screen.y,band,zone.h);ctx.fillRect(screen.x+zone.w-band,screen.y,band,zone.h);ctx.fillRect(screen.x,screen.y,zone.w,band*.65);ctx.fillRect(screen.x,screen.y+zone.h-band*.65,zone.w,band*.65);}
 if(zone.v040dBug){const frame=Math.floor(elapsed*11);for(let i=0;i<7;i++){const seed=(frame*17+i*41+zone.x*.01+zone.y*.02),x=screen.x+((seed*37)%Math.max(1,zone.w-90)),y=screen.y+((seed*19)%Math.max(1,zone.h-45));ctx.fillStyle=i%2?'rgba(241,231,214,.12)':'rgba(82,18,55,.22)';ctx.fillRect(x,y,50+(i%3)*28,8+(i%2)*10);}}
 ctx.restore();
};
const v040dLegacyDrawObject=drawObject;
drawObject=function(object,screen){if(object.structureNode&&object.nodeType==='error'){const pulse=.5+.5*Math.sin(elapsed*9);ctx.save();ctx.translate(screen.x+object.w*.5,screen.y+object.h*.5);ctx.fillStyle='rgba(18,10,20,.92)';ctx.fillRect(-25,-25,50,50);ctx.strokeStyle='rgba(255,255,255,'+(.55+.4*pulse)+')';ctx.strokeRect(-24,-24,48,48);ctx.font='800 26px monospace';ctx.fillStyle='#fff';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('�',0,1);ctx.restore();return;}v040dLegacyDrawObject(object,screen);};
function v040dActivePlan(){for(const id of structureEncounterActive){const plan=structureEncounterPlans.get(id);if(plan)return plan;}return null;}
function v040dDamageEnemies(plan,test,ratio){for(const enemy of enemies)if(enemy.structureRoomId===plan.roomId&&!enemy.structureDormant&&test(enemy)){enemy.hp-=Math.max(3,enemy.maxHp*ratio);enemy.flash=Math.max(enemy.flash||0,.12);}}
function v040dUpdateMechanics(dt){
 for(const enemy of enemies)if(enemy.structureDormant&&enemy.structureWakeAt&&elapsed>=enemy.structureWakeAt){enemy.structureDormant=false;enemy.structureWakeAt=0;enemy.clotStun=0;enemy.navCheck=0;}
 const plan=v040dActivePlan();if(!plan||!plan.startedAt)return;const room=STRUCTURE_ROOMS.find(item=>item.id===plan.roomId);if(!room)return;v040dMechanicTick-=dt;
 if(plan.rare==='contamination'&&v040dMechanicTick<=0){v040dMechanicTick=.75;for(const pool of plan.pools||[]){const dx=player.x-pool.x,dy=player.y-pool.y;if(dx*dx+dy*dy<pool.r*pool.r)applyPlayerDamage(1,.55);v040dDamageEnemies(plan,enemy=>{const ex=enemy.x-pool.x,ey=enemy.y-pool.y;return ex*ex+ey*ey<pool.r*pool.r;},.05);}}
 if(plan.rare==='sterilization'){const cycle=(elapsed-plan.startedAt)%4.5,index=Math.floor((elapsed-plan.startedAt)/4.5),active=cycle>=1&&cycle<1.55,stripe=plan.stripe;if(active&&plan.cycleHit!==index){const playerIn=stripe.vertical?Math.abs(player.x-(room.x+stripe.offset*room.w))<70:Math.abs(player.y-(room.y+stripe.offset*room.h))<55;if(playerIn)applyPlayerDamage(1,.6);v040dDamageEnemies(plan,enemy=>stripe.vertical?Math.abs(enemy.x-(room.x+stripe.offset*room.w))<70:Math.abs(enemy.y-(room.y+stripe.offset*room.h))<55,.12);plan.cycleHit=index;}}
 if(plan.rare==='compression'){const cycle=(elapsed-plan.startedAt)%6,index=Math.floor((elapsed-plan.startedAt)/6),amount=cycle<2?0:cycle<3.5?(cycle-2)/1.5:cycle<5?(5-cycle)/1.5:0,margin=amount*135;if(amount>.75&&plan.cycleHit!==index){const outside=Math.abs(player.x-room.x)>room.w*.5-margin-30||Math.abs(player.y-room.y)>room.h*.5-margin*.65-30;if(outside)applyPlayerDamage(1,.6);v040dDamageEnemies(plan,enemy=>Math.abs(enemy.x-room.x)>room.w*.5-margin-25||Math.abs(enemy.y-room.y)>room.h*.5-margin*.65-25,.10);plan.cycleHit=index;}}
}
const v040dLegacyRenderCompactRelics=renderCompactRelics;
renderCompactRelics=function(){v040dLegacyRenderCompactRelics();const root=document.getElementById('relicHud'),item=V040D_ERROR_ITEMS[v040dErrorItem];if(root&&item)root.insertAdjacentHTML('beforeend','<span class="relicHudItem errorItem" title="'+item.name+'">'+item.icon+'</span>');};
const v040dLegacyPauseSummary=renderPauseSummary;
renderPauseSummary=function(){v040dLegacyPauseSummary();const item=V040D_ERROR_ITEMS[v040dErrorItem];if(!item||!ui.pauseSummary)return;const sections=ui.pauseSummary.querySelectorAll('.pauseSection');const target=sections[sections.length-1];if(target)target.insertAdjacentHTML('beforeend',pauseItem(item.icon,item.name+' · 오류',item.desc));};
const v040dLegacyCreditSpecimens=creditSpecimens;
creditSpecimens=function(value){let amount=value;if(v040dErrorItem==='duplicateSpecimen'&&Math.random()<.35)amount*=2;return v040dLegacyCreditSpecimens(amount);};
const v040dLegacyApplyDamage=applyPlayerDamage;
applyPlayerDamage=function(raw,invuln=.45){if(v040dErrorItem==='missingDamage'&&Math.random()<.18){player.invuln=Math.max(player.invuln||0,.25);sfx('crit');return false;}if(v040dErrorItem==='unwrittenDeath'&&!v040dErrorGuardUsed&&player.hp<=1){v040dErrorGuardUsed=true;player.hp=1;player.invuln=.9;renderHeartHud();sfx('growth');return false;}return v040dLegacyApplyDamage(raw,invuln);};
const v040dLegacySwapWeapon=structureSwapWeapon;
structureSwapWeapon=function(){const before=primaryWeaponKey;v040dLegacySwapWeapon();if(v040dErrorItem==='wrongReference'&&primaryWeaponKey&&primaryWeaponKey!==before&&weapons[primaryWeaponKey])weapons[primaryWeaponKey].timer=0;};
function v040dPlacePlayerAtStart(){const start=STRUCTURE_ROOMS.find(room=>room.id==='start');if(!start)return;player.x=start.x;player.y=start.y;camera.x=start.x;camera.y=start.y;renderCamera.x=start.x;renderCamera.y=start.y;structureVisitedRooms=new Set(['start']);structureLastRoom='start';}
structureAdvanceFloor=function(){structureFloor++;structureClearFloorObjects();generateWorld();rebuildObjectGrid();buildMinimapStatic();v040dPlacePlayerAtStart();spawnTimer=999999;renderMinimap();updateCompactHud();syncActionButton();};
const v040dLegacyReset=reset;
reset=function(){structureFloor=1;v040dBugUsed=false;v040dBugFloor=v040dDebugBug?1:null;v040dErrorItem=null;v040dErrorGuardUsed=false;v040dLegacyReset();if(!v040dDebugBug&&Math.random()<.01)v040dBugFloor=3+Math.floor(Math.random()*4);v040dPlacePlayerAtStart();buildMinimapStatic();renderMinimap();updateCompactHud();syncActionButton();};
const v040dLegacyUpdate=update;
update=function(dt){v040dLegacyUpdate(dt);v040dUpdateMechanics(dt);if(v040dErrorItem==='overflowClock'&&primaryWeaponKey&&weapons[primaryWeaponKey])weapons[primaryWeaponKey].timer-=dt*.35;};
