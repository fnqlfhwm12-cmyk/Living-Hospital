# AGENTS.md

이 파일은 Living Hospital 저장소를 수정하는 ChatGPT, Codex 및 기타 개발 에이전트가 따라야 하는 작업 규칙입니다.

## 작업 시작 전 필수 확인

1. `README.md`를 읽습니다.
2. `docs/PROJECT_STATUS.md`, `docs/PROJECT_RULES.md`, `docs/CHANGELOG.md`, `docs/ROADMAP.md`를 읽습니다.
3. 저장소에서 가장 최신 버전의 `Living_Hospital_*.html`을 확인합니다.
4. 사용자가 요청한 변경과 현재 코드가 충돌하는지 먼저 분석합니다.
5. 요청 범위를 임의로 넓히지 않습니다.

## 버전 관리

- 현재 기준선은 `Living_Hospital_v0.3.3.t.html`입니다.
- v0.3.3 수정 계열은 알파벳 접미사를 순차적으로 사용합니다.
- 기존 버전 파일을 덮어쓰거나 삭제하지 않습니다.
- 새 버전은 최신 파일을 복제한 뒤 수정합니다.
- 실제 다음 버전명은 작업 시작 시 저장소의 최신 상태를 기준으로 확정합니다.
- 코드 변경 커밋에는 버전과 핵심 변경을 포함합니다.
  - 예: `v0.3.3.u: 심장박동 추가타 타이밍 조정`

## 브랜치와 반영

- 일반 작업은 `develop` 또는 별도 작업 브랜치에서 진행합니다.
- `main`은 검토가 끝난 기준 버전과 문서를 보관합니다.
- 기능 변경은 가능하면 Pull Request로 검토한 뒤 `main`에 반영합니다.
- 긴급한 문서 정리 외에는 검증되지 않은 게임 코드를 바로 `main`에 덮어쓰지 않습니다.

## 기술 제약

- 단일 HTML 구조를 성급하게 여러 파일로 분리하지 않습니다.
- 주 테스트 환경은 iPhone 가로 화면입니다.
- 화면 회전, safe-area, 터치 입력, Safari 계열 웹뷰 동작을 유지합니다.
- 매 프레임 생성되는 객체, 파티클, 오디오 노드, 타이머를 불필요하게 늘리지 않습니다.
- 기능 추가 전후에 프레임 저하, 발열, 객체 누수 가능성을 검토합니다.
- 디버그 코드와 테스트용 수치는 출시 기준 코드와 구분합니다.
- GitHub Pages 설정이나 배포 워크플로는 사용자의 명시적 요청 없이 추가하지 않습니다.

## 고정된 디자인 방향

- 장르: 뱀서류 기반 액션 로그라이크
- 톤: 귀엽지만 그로테스크한 병원·실험실
- 목표 플레이 타임: 30~45분
- 게임은 설명을 과도하게 제공하지 않으며, 텍스트는 짧고 모호하며 묵직하게 유지합니다.
- 메인 장기는 한 번에 1개만 착용하는 교체식 구조입니다.
- 기본 무기는 개성이 있으나 완성형 성능이 아니어야 합니다.
- 단일 대상 공격은 강하고, 광역 공격은 상대적으로 약해야 합니다.
- 새 콘텐츠보다 전투 판독성, 조작감, 밸런스, 성능 안정성을 우선합니다.

## 변경 검토 체크리스트

- 요청한 기능이 실제로 작동하는가
- 기존 기능이 깨지지 않았는가
- 이전 버전과 비교해 의도하지 않은 수치 변화가 없는가
- iPhone 가로 화면에서 UI가 잘리지 않는가
- 적, 투사체, 파티클이 누적되지 않는가
- 사운드가 중첩되거나 과도하게 재생되지 않는가
- `docs/CHANGELOG.md`가 갱신되었는가
- `docs/PROJECT_STATUS.md`의 현재 버전이 갱신되었는가

## 금지 사항

- 사용자 승인 없이 핵심 콘셉트 변경
- 기존 버전 삭제 또는 동일 파일명 덮어쓰기
- 테스트하지 않은 대규모 리팩터링과 기능 추가를 한 번에 결합
- 설명 문구를 친절한 튜토리얼 형태로 과도하게 확장
- 성능 비용을 검토하지 않은 대량 파티클·다중 루프 추가
